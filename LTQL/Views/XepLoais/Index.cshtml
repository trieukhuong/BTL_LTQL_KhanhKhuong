@model IEnumerable<LTQL.Models.XepLoai>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DoanVien.Ma_dv)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Năm_hoc)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nhan_xet)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Xep_loai)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DoanVien.Ma_dv)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Năm_hoc)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nhan_xet)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Xep_loai)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
